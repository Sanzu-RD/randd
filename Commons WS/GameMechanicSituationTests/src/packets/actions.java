package packets;

public class actions {

	/*
	 * Either :
	 * 
	 * - cast a spell  ---> source, spellID, targetCell
	 * - move          ---> source, targetCell
	 * - pass turn	   ---> source
	 * - use an item ? ---> source, itemID, targetCell
	 * - swap with a player on the bench ? 	---> source, target
	 * 
	 */
	
}
